# Development-only tools. Not needed to run the app.
#
#     pip install -r requirements-dev.txt

-r requirements.txt

# Formatting and linting. Configured in pyproject.toml so everyone's editor
# agrees and diffs stay about behaviour, not whitespace.
ruff==0.6.*
